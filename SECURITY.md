# 安全说明

程序离线运行，读取本地皮肤、保存本地偏好，不上传图片或采集全局键盘输入。

外部皮肤受目录边界、清单、大小和尺寸校验约束；无效高清资源会回退，用户设置的未来版本不会被旧程序静默改写。

若发现安全问题，请私下联系仓库维护者。不要在公开Issue中上传凭据、完整设置、个人路径或私人图片；普通问题请提供最小复现步骤并裁剪截图。

本仓库不包含自动更新器。测试包未做Developer ID签名或Apple公证，请只运行可信来源的包并核对SHA-256，不要关闭系统范围的安全检查。

Windows PowerShell 5.1 打包流程已实机测试；CI与打包成功仍不能证明真实桌面行为或跨显示器缩放正常。
