# mac-test-20260904

用户已在主要Apple Silicon Mac实测确认“按住拖动和动作衔接”可用。Windows仅有自动测试/构建结果，本轮未实机验收。

源实现检查：51项前端测试、macOS Rust测试、macOS及Windows CI构建通过。素材处理工具的18项本地测试也通过，但这些私有制作工具不在公开快照中，不能在本仓库重复运行。

主要验证点：

1. 正面蹲坐、轻摆尾，动作间体型和边缘稳定。
2. 悬停不触发；按下翻肚皮、按住拖动、松手收势，无左右跑动替换。
3. 舔爪、踩奶、伸展完整播放并自然回坐。
4. 退出重启、位置记忆、托盘/菜单、换肤失败回退。
5. 外接屏和缩放变化；反馈应包含Mac芯片、系统版本、缩放和最小复现步骤。

Mac测试ZIP标识：`鳌拜·桌面宠物-macOS-arm64-test-20260904.zip`。

- 仅Apple Silicon arm64，macOS12+，不适用Intel Mac。
- ZIP SHA-256：`7ad6d049a59cc6762a0e78c8add18f1a2aa0a6b0d35a99b8858368d693e743cc`。
- 已在新的本机临时目录解压、校验6个文件、确认command和二进制执行位、启动进程和208×208窗口。
- 未模拟另一台Mac、全新系统账号或聊天下载后的隔离属性。
- 版本元数据保留0.4.1，以快照编号区分旧版；本仓库不会自动下载或运行测试ZIP。

回滚：退出当前实例，运行自行保留的上一版本。不要为回滚删除用户配置。
